# Copier en .env et remplir. Lu par config.Load() ET par le Makefile.
# La valeur ci-dessous correspond au Postgres de docker-compose.yml (port 5433).
DATABASE_URL=postgres://flowlio:flowlio@localhost:5433/flowlio?sslmode=disable
ADDR=:8080
ENV=dev

# Prod : jamais renseigné dans ce fichier, jamais commité.
# DATABASE_URL_PROD=

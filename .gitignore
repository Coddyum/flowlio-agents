# Binaires
/bin/
/tmp/
flowlio-ia

# Artefacts de release produits par goreleaser
/dist/

# Env
.env
.env.*
!.env.example

# Go
*.test
*.out
coverage.*

# OS / éditeurs
.DS_Store

# Worktrees temporaires créés par les agents
.claude/worktrees/


# Session handoff and its two append-only companions. Working notes, written in French, for
# whoever picks the project up next — not documentation, and not part of what this repo publishes.
next.md
docs/decisions.md
docs/lecons.md

# Memory system copied from another repo as a design reference. It carries that repo's real
# security findings and internal state — it is somebody else's context, not this project's, and
# nothing in it belongs in a public repository.
docs/memory-agents/

# The repository's own MCP configuration is per-machine: `flowlio connect` writes it,
# and it names the instance this checkout is pointed at. Committing one would hand every
# clone somebody else's board.
.mcp.json
